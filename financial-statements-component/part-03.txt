Groups.map(r=>`<tr><td>${r.category}</td><td class="n">${fsMoney(r.amount)}</td></tr>`).join('')
  const warnings=d.issues.map(x=>`<li>${x}</li>`).join('')
  return `<!doctype html><html><head><meta charset="utf-8"><title>Roma's Donuts Financial Statements</title><style>body{font-family:Arial,sans-serif;color:#172033;margin:28px}h1{margin:0;font-size:22px}h2{margin-top:28px;font-size:16px;border-bottom:2px solid #222;padding-bottom:6px}.meta{font-size:11px;color:#666;margin:6px 0 18px}table{width:100%;border-collapse:collapse;font-size:11px}td,th{padding:7px;border-bottom:1px solid #ddd;text-align:left}.n{text-align:right}.total{font-weight:bold;border-top:2px solid #222}.warn{background:#fff8e7;border:1px solid #e3c97d;padding:10px;font-size:10px}@media print{@page{size:A4;margin:15mm}.no-print{display:none}}</style></head><body><h1>ROMA'S DONUTS</h1><div>Financial Statements & Supporting Schedules</div><div class="meta">Reporting period: ${d.start} to ${d.end} · Prior period: ${d.priorStart} to ${d.priorEnd} · Generated ${new Date().toLocaleString('en-PH')}</div>${d.issues.length?`<div class="warn"><strong>Data quality notice</strong><ul>${warnings}</ul></div>`:''}<h2>Statement of Profit or Loss</h2><table><tr><td>Revenue</td><td class="n">${fsMoney(d.pl.revenue)}</td></tr><tr><td>Cost of Goods Sold</td><td class="n">${fsDisplayMoney(d.pl.cogs)}</td></tr><tr class="total"><td>Gross Profit</td><td class="n">${fsDisplayMoney(d.pl.gross)}</td></tr>${expenseRows}<tr class="total"><td>Total Operating Expenses</td><td class="n">${fsMoney(d.pl.opex)}</td></tr><tr class="total"><td>Net Operating Result</td><td class="n">${fsDisplayMoney(d.pl.net)}</td></tr></table><h2>Financial Position – Supporting Schedules</h2><table><tr><td>Indicative Inventory Value</td><td class="n">${fsMoney(d.inventoryValue)}</td></tr><tr><td>Trade Receivables (delivery invoices)</td><td class="n">${fsMoney(d.receivableTotal)}</td></tr><tr><td>Open Company Payables</td><td class="n">${fsMoney(d.payableTotal)}</td></tr></table><p style="font-size:9px;color:#666">A formal Balance Sheet and Statement of Cash Flows are not presented unless the underlying opening balances, complete cash records, and all balance-sheet accounts are available and reliable.</p></body></html>`
 }
 const printReport=()=>{ const popup=window.open('','_blank','noopener,noreferrer'); if(!popup){showToast?.('Allow pop-ups to print the report.');return} popup.document.write(buildPrintHtml()); popup.document.close(); popup.focus(); setTimeout(()=>popup.print(),150) }
 const downloadCsv=()=>{ if(!data)return; const rows=[['Roma\'s Donuts Financial Statements'],['Period',data.start,data.end],['Last updated',lastUpdated?.toISOString()||''],[],['Statement of Profit or Loss','Current','Prior'],['Revenue',data.pl.revenue,data.priorPl.revenue],['Cost of Goods Sold',data.pl.cogs??'Unavailable',data.priorPl.cogs??'Unavailable'],['Gross Profit',data.pl.gross??'Unavailable',data.priorPl.gross??'Unavailable'],...data.expenseGroups.map(r=>[`Expense - ${r.category}`,r.amount,'']),['Total Operating Expenses',data.pl.opex,data.priorPl.opex],['Net Operating Result',data.pl.net??'Unavailable',data.priorPl.net??'Unavailable'],[],['Supporting Schedules'],['Indicative Inventory Value',data.inventoryValue],['Trade Receivables',data.receivableTotal],['Open Company Payables',data.payableTotal],[],['Data Quality Notices'],...data.issues.map(x=>[x])]; const csv=rows.map(row=>row.map(fsCsvCell).join(',')).join('\r\n'); const blob=new Blob([csv],{type:'text/csv;charset=utf-8'}); const url=URL.createObjectURL(blob); const a=document.createElement('a');a.href=url;a.download=`Romas_Financial_Statements_${data.start}_to_${data.end}.csv`;a.click();URL.revokeObjectURL(url) }
 const downloadPdf=async()=>{ if(!data)return; try { const {jsPDF}=await import('jspdf'); const doc=new jsPDF({unit:'mm',format:'a4'}); let y=16; const line=(label,value,bold=false)=>{ if(y>280){doc.addPage();y=16} doc.setFont('helvetica',bold?'bold':'normal');doc.setFontSize(bold?10:9);doc.text(label,15,y);doc.text(fsDisplayMoney(value),195,y,{align:'right'});y+=6 }; doc.setFont('helvetica','bold');doc.setFontSize(16);doc.text("ROMA'S DONUTS",15,y);y+=7;doc.setFontSize(10);doc.text('Financial Statements & Supporting Schedules',15,y);y+=6;doc.setFont('helvetica','normal');doc.setFontSize(8);doc.text(`Period: ${data.start} to ${data.end} | Generated: ${new Date().toLocaleString('en-PH')}`,15,y);y+=9;if(data.issues.length){doc.setFont('helvet